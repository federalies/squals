/**
 * PIPELINE
 * look at all the changed content files
 * (test) ensure content files are passable via ste confg
 * (build) the content page?
 * (deploy) save the content page in the s3 bucket and issue invalidation?
 */
